{
  "name": "real-estate-platform-heroku",
  "version": "1.0.0",
  "description": "Real Estate Platform - Heroku Deployment",
  "scripts": {
    "start": "cd backend && npm start",
    "postinstall": "cd backend && npm install",
    "heroku-postbuild": "cd backend && npm install"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}
